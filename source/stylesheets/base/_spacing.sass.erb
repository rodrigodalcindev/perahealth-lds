// Spacing

.b-block
  @include display(flex)
  @include align-items(center)
  background: $marine
  border-radius: $b-border-radius
  height: 50px
  width: 100%
  &:before
    background: $spring
    border-radius: 0 $b-border-radius $b-border-radius 0
    content: ''
    height: 50px
    position: fixed
    width: 100%
    z-index: 0

.b-block-container
  z-index: 1

<% design_token("spacing").each do |prop| %>

.b-block--<%= prop.name %>
  padding-left: $b-<%= prop.name %>

<% end %>
